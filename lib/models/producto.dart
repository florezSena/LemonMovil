class Producto{
  int idProducto;
  String nombre;
  double cantidad;
  double precio;
  String? descripcion;
  int estado;
  Producto(this.idProducto, this.nombre, this.cantidad,this.precio, this.descripcion, this.estado);
}