import 'package:flutter/material.dart';

class VentasIndex extends StatelessWidget {
  const VentasIndex({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ventas proximamente..."),
    );
  }
}